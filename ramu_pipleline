pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Get some code from a GitHub repository
                git 'https://github.com/ramubdpl123/DevOpsClassCodes.git'

            }
        }
        stage('Compile') {
            steps {
                // Get some code from a GitHub repository
                
                sh 'mvn compile'
            }
        }
        stage('CodeReview') {
            steps {
                // Get some code from a GitHub repository
                
                sh 'mvn pmd:pmd'
            }
        }
        stage('UnitTest') {
            steps {
                // Get some code from a GitHub repository
                
                sh 'mvn test'
            }
        }
        stage('MetriCheck') {
            steps {
                // Get some code from a GitHub repository
                
                sh 'mvn cobertura:cobertura -Dcobertura.report.format=xml'
            }
        }
        stage('Package') {
            steps {
                // Get some code from a GitHub repository
                
                sh 'mvn package'
            }
        }
    }
}
